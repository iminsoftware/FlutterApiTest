<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.imin.hardware">
    
    <!-- Android 11+ package visibility for iMin Scale Service -->
    <queries>
        <package android:name="com.imin.peripherservice" />
    </queries>
    
    <!-- Display permissions -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    
    <!-- Camera permission for scanning -->
    <uses-permission android:name="android.permission.CAMERA"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false"/>
    
    <!-- USB permissions for light control -->
    <uses-feature android:name="android.hardware.usb.host" android:required="false"/>
    
    <application>
        <!-- Floating Window Service -->
        <service
            android:name=".floatingwindow.FloatingWindowService"
            android:enabled="true"
            android:exported="false" />
        
        <!-- Camera Scan Activity -->
        <activity
            android:name=".camera.FlutterCaptureActivity"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.NoTitleBar.Fullscreen"
            android:exported="false" />
    </application>
</manifest>
